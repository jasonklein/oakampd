#gigs-listings
  %table
    %tbody
      - @gigs.each do |gig|
        - if @first_ids.include? gig.id
          %tr.first-of-the-month
            %td
              = display_month(gig)
            %td
            %td
            %td
        %tr
          %td.showdate
            = display_showdate(gig)
          %td.band-name
            = gig.band
          %td.venue-details
            = display_venue_details(gig)
          %td.price
            %span
              = display_price(gig)